const { ObjectId } = require("mongodb");
const db = require('./../app');
const { checkString } = require('./../utils');
const { surgeriesCollectionName } = require('./../utils');


async function getAll() {}


module.exports = {
    getAll
  }